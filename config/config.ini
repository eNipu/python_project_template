[DEFAULT]
; This is the default language used by the system
; Available languages: en, fr, de, es, it, nl, pl, pt, ru, sv, tr, uk, zh
; Default: en
language = en
PORT = 8080
host = localhost
; This is the default timezone used by the system
; Default: UTC
timezone = UTC
; This is the default timezone used by the system
; Default: UTC  
date_format = d/m/Y
; This is the default timezone used by the system
; Default: UTC
time_format = H:i